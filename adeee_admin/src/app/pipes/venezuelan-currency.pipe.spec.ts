import { VenezuelanCurrencyPipe } from './venezuelan-currency.pipe';

describe('VenezuelanCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new VenezuelanCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
